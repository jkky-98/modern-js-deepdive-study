var num = 2;
var kind;
console.log(kind);
// declare a variable and assign a value to it

if (num > 0) {
  kind = "positive";
}
if (num < 0) {
  kind = "negative";
}
if (num == 0) {
  kind = "zero";
}
// this is a conditional statement that assigns a value to the variable kind
console.log(kind);
// print the value of kind to the console

// for loop
for (var i = 0; i < 10; i++) {
  console.log(i);
}
// first declare a variable i and assign it a value of 0
// then check if i is less than 10
// if it is, print the value of i to the console
// then increment i by 1
// repeat until i is no longer less than 10
// this is a for loop
